[phases.setup]
nixPkgs = ["php84", "php84Extensions.pdo_pgsql"]

[start]
cmd = "php bin/console cache:clear --env=prod && php bin/console doctrine:migrations:migrate --no-interaction && php -S 0.0.0.0:$PORT -t public"